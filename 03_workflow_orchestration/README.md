## Week 3
* Workflow orchestration with Kestra

For this week's setup, I'd recommend setting up the Gemini api key, openai api key etc. as user secret in Github codespace, instead of using just the command line. 

Why: using `echo` on terminal, you'll need to run it every time the codespace is restarted.  